const numPoints = 1000
const circleSettings = {
    maxX : window.innerWidth,
    maxY : window.innerHeight - window.innerHeight/3,
    maxR : 50,
}

const heartSettings = {
    cX: window.innerWidth/2,
    cY : window.innerHeight/2 - 100,
    r: 25,
}